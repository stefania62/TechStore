package app.finalprojectfx.db.model;

//enum is a special class which allows programmers to define some variables and use them as constants
public enum UserRole {
    CASHIER,
    MANAGER,
    ADMINISTRATOR,

}
